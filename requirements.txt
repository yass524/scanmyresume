fastapi==0.112.2
uvicorn[standard]==0.30.6
python-multipart>=0.0.9
python-dotenv>=1.0.1
groq>=0.31.0
SQLAlchemy>=2.0.31
bcrypt==4.0.1
passlib[bcrypt]>=1.7.4

pytest>=8.3.0

# File parsing
pdfminer.six>=20221105
python-docx>=0.8.11

# PDF export
reportlab>=4.0.0

# Optional ML deps (sentence-transformers / scikit-learn / spacy) are intentionally
# excluded from default deploy requirements to keep serverless bundle size small.
# Add them back only for environments where semantic matching is enabled.
psycopg[binary]>=3.2
